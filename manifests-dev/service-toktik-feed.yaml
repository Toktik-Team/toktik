apiVersion: v1
kind: Service
metadata:
  labels:
    app: toktik-feed
  name: toktik-feed
  namespace: toktik-service-bundle
spec:
  ports:
    -   name: grpc
        port: 40129
        protocol: TCP
        targetPort: 40129
  selector:
    name: toktik-feed
    branch: dev
  type: ClusterIP
